"use client";
import { useEffect, useState } from "react"; import Nav from "@/components/Nav"; import RequireRole from "@/components/RequireRole"; import { supabase } from "@/lib/supabaseClient";
export default function AuditLogs(){const [logs,setLogs]=useState([]);useEffect(()=>{load();},[]);async function load(){const {data}=await supabase.from('audit_logs').select('*, profiles(email,full_name)').order('created_at',{ascending:false}).limit(300);setLogs(data||[]);}return <RequireRole roles={["system_admin"]}><Nav role="system_admin"/><main className="page"><h1>View Audit Logs</h1><div className="card"><table><thead><tr><th>Date</th><th>User</th><th>Action</th><th>Details</th></tr></thead><tbody>{logs.map(l=><tr key={l.id}><td>{new Date(l.created_at).toLocaleString()}</td><td>{l.profiles?.email||l.user_id||'-'}</td><td>{l.action}</td><td>{l.details}</td></tr>)}</tbody></table></div></main></RequireRole>}
