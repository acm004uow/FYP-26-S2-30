"use client";
import { useEffect, useState } from "react"; import Nav from "@/components/Nav"; import RequireRole from "@/components/RequireRole"; import { supabase } from "@/lib/supabaseClient";
export default function AdminDashboard(){const [stats,setStats]=useState({users:0,logs:0,audit:0});useEffect(()=>{load();},[]);async function load(){const [{count:users},{count:logs},{count:audit}]=await Promise.all([supabase.from('profiles').select('id',{count:'exact',head:true}),supabase.from('security_logs').select('id',{count:'exact',head:true}),supabase.from('audit_logs').select('id',{count:'exact',head:true})]);setStats({users:users||0,logs:logs||0,audit:audit||0});}return <RequireRole roles={["system_admin"]}><Nav role="system_admin"/><main className="page"><h1>System Admin Dashboard</h1><div className="grid2"><div className="card"><h2>{stats.users}</h2><p>User Accounts</p></div><div className="card"><h2>{stats.logs}</h2><p>Security Logs</p></div><div className="card"><h2>{stats.audit}</h2><p>Audit Logs</p></div></div></main></RequireRole>}
