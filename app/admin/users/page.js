"use client";
import { useEffect, useState } from "react"; import Nav from "@/components/Nav"; import RequireRole from "@/components/RequireRole"; import StatusBadge from "@/components/StatusBadge"; import { supabase } from "@/lib/supabaseClient";
const empty={email:'',password:'Password123!',full_name:'',role:'manager'};
export default function AdminUsers(){const [form,setForm]=useState(empty);const [users,setUsers]=useState([]);const [search,setSearch]=useState('');useEffect(()=>{load();},[]);async function load(){const {data}=await supabase.from('profiles').select('*').order('created_at',{ascending:false});setUsers(data||[]);}async function create(e){e.preventDefault();const res=await fetch('/api/admin/create-user',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const json=await res.json();if(!res.ok)return alert(json.error);alert('User account created');setForm(empty);load();}async function role(u,role){await supabase.from('profiles').update({role}).eq('id',u.id);await supabase.from('audit_logs').insert({action:'manage_user_roles_permissions',details:`${u.email}: ${role}`});load();}async function deactivate(u){await supabase.from('profiles').update({status:'suspended'}).eq('id',u.id);await supabase.from('audit_logs').insert({action:'deactivate_user_account',details:u.email});load();}async function reset(u){const new_password=prompt('Enter new temporary password','Password123!');if(!new_password)return;const res=await fetch('/api/admin/reset-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({user_id:u.id,new_password})});const json=await res.json();if(!res.ok)return alert(json.error);alert('Password reset successfully');}const filtered=users.filter(u=>[u.full_name,u.email,u.role,u.status].join(' ').toLowerCase().includes(search.toLowerCase()));return <RequireRole roles={["system_admin"]}><Nav role="system_admin"/><main className="page"><h1>System Admin User Management</h1><div className="grid2"><div className="card"><h2>Create Manager / Department Staff Accounts</h2><form className="grid" onSubmit={create}><input className="input" placeholder="Full name" value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})} required/><input className="input" type="email" placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/><input className="input" placeholder="Temporary password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/><select className="input" value={form.role} onChange={e=>setForm({...form,role:e.target.value})}><option value="manager">Manager</option><option value="department_staff">Department Staff</option><option value="staff_member">Staff Member</option><option value="system_admin">System Admin</option></select><button className="btn">Create Account</button></form></div><div className="card"><h2>Search Users</h2><input className="input" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search user accounts"/></div></div><div className="card"><h2>Manage Roles, Reset Passwords, Deactivate Accounts</h2><table><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Status</th><th>Actions</th></tr></thead><tbody>{filtered.map(u=><tr key={u.id}><td>{u.full_name}</td><td>{u.email}</td><td><select className="input" value={u.role} onChange={e=>role(u,e.target.value)}><option value="manager">manager</option><option value="department_staff">department_staff</option><option value="staff_member">staff_member</option><option value="system_admin">system_admin</option></select></td><td><StatusBadge value={u.status}/></td><td className="row"><button className="btn warning" onClick={()=>reset(u)}>Reset Password</button><button className="btn danger" onClick={()=>deactivate(u)}>Deactivate</button></td></tr>)}</tbody></table></div></main></RequireRole>}
