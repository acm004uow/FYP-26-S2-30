"use client";
import { useEffect, useState } from "react"; import Nav from "@/components/Nav"; import RequireRole from "@/components/RequireRole"; import { supabase } from "@/lib/supabaseClient";
export default function StaffNotifications(){const [items,setItems]=useState([]);useEffect(()=>{load();},[]);async function load(){const {data:{user}}=await supabase.auth.getUser(); if(!user)return; const {data}=await supabase.from('notifications').select('*').eq('user_id',user.id).order('created_at',{ascending:false}); setItems(data||[]);} async function mark(id){await supabase.from('notifications').update({is_read:true}).eq('id',id);load();} return <RequireRole roles={["staff_member"]}><Nav role="staff_member"/><main className="page"><h1>Task Assignment Notifications</h1>{items.map(n=><div className="card" key={n.id}><h3>{n.title} {!n.is_read&&<span className="badge pending">unread</span>}</h3><p>{n.message}</p><button className="btn secondary" onClick={()=>mark(n.id)}>Mark Read</button></div>)}</main></RequireRole>}
