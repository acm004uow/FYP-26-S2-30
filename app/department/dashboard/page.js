"use client";
import { useEffect, useState } from "react"; import Nav from "@/components/Nav"; import RequireRole from "@/components/RequireRole"; import { supabase } from "@/lib/supabaseClient";
export default function DepartmentDashboard(){const [stats,setStats]=useState({submitted:0,pending:0,approved:0,completed:0});useEffect(()=>{load();},[]);async function load(){const {data:{user}}=await supabase.auth.getUser(); if(!user)return; const {data}=await supabase.from('task_requests').select('status').eq('created_by',user.id); const rows=data||[]; setStats({submitted:rows.length,pending:rows.filter(r=>r.status==='pending').length,approved:rows.filter(r=>r.status==='approved').length,completed:rows.filter(r=>r.status==='completed').length});}return <RequireRole roles={["department_staff"]}><Nav role="department_staff"/><main className="page"><h1>Department Staff Dashboard</h1><div className="grid2"><div className="card"><h2>{stats.submitted}</h2><p>Submitted Tasks</p></div><div className="card"><h2>{stats.pending}</h2><p>Pending Approval</p></div><div className="card"><h2>{stats.approved}</h2><p>Approved</p></div><div className="card"><h2>{stats.completed}</h2><p>Completed</p></div></div></main></RequireRole>}
